#include <stdio.h>

int main(int argc, char *argv[])
{

	int n; 
  int successivo;

	do
	{									  
		printf("inserisci un numero: ");
		scanf("%d", &n);				   
	} while (n <= 0);					 

	successivo = n + 1;
	printf("il successivo vale: %d", successivo);

	return 0;
}
