#include <stdio.h>

int main()
{
    int a[4];
    
    for(int i=0; i<4; i++)
    {
        printf("Inserisci %d° numero: ",i+1);
        scanf("%d", &a[i]);
    }
    printf("Il vettore è composto  da: ");
    
    for(int i=0; i<4; i++)
    {
        printf("%d ",a[i]);
    }

    return 0;
}
