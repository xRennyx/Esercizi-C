#include <stdio.h>

int main(int argc, char *argv[])
{
    int N;
    
    do
    {
        printf("Inserisci N: ");
        scanf("%d", &N);
    }while(N<=0);
    
    for(int i=0; i<=N; i++)
    {
        if(i%2==0)
        {
            printf("%d\n", i);
        }
    }
return 0;    
}
