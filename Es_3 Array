#include <stdio.h>

int main(int argc, char *argv[])
{
    int a[10];
    
    for(int i=0; i<10; i++)
    {
        printf("Inserisci %d° numero: ",i+1);
        scanf("%d", &a[i]);
    }
    printf("Numeri Pari: ");
    
    for(int i=0; i<10; i++)
    {
        if(a[i]%2==0)
        {
            printf("%d ",a[i]);
        }    
    }
    printf("\n");
    printf("Numeri Dispari: ");
        for(int i=0; i<10; i++)
    {
        if(a[i]%2!=0)
        {
            printf("%d ",a[i]);
        }    
    }

    return 0;
}
